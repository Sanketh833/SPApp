
server:
  port: 8082
  
spring:
  application:
    name: SPFOOD
  cloud:
    consul:
      config:
        enabled: true 
        prefix: config
        default-context: application
        profile-separator: '::'
        data-key: data
        format: yaml
      
      
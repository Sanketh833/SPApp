#server:
#  port: 8085
  
spring:
  application:
    name: SPROOMS
  cloud:
    consul:
      config:
        enabled: true
        prefix: config
        default-context: application
        profile-separator: '::'
        data-key: data
        format: yaml    
  